Before to use it you have to be sure to have done the followed step:

1) check if the 'output' folder is already created in 'inference iot' folder otherwise(you're fucked up) you create it.
2) in 'Roboflow_inference.py' change the 'output folder'path related to 'output' folder path in your PC

To run the script without imprecate:

1) Go in 'inference iot' folder via terminal (with cd /.../inference iot/)
2) Use the command: python.exe bridge.py

Don't worry if you will be classify as bee, slug or ladybird, you are not.
It is only a stupid sexy AI.